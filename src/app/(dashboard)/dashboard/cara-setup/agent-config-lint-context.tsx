"use client";

import type { ReactNode } from "react";

/** Legacy wrapper — lint hints removed from dashboard setup UI. */
export function AgentConfigLintProvider({ children }: { children: ReactNode }) {
  return children;
}
