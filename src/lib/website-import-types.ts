export type WebsiteImportLocation = {
  label: string;
  address: string;
  eircode: string;
  phone?: string;
  email?: string;
};

export type WebsiteImportFaq = { question: string; answer: string };
